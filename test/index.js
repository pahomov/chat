'use strict';

const request = require('request');
const should = require('should');

describe('section descr', function() {
  describe('dummy test', function() {
    it('always true', function(done) {
      done();
    });
  });
});
